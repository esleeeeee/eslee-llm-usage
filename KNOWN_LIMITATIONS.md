# 알려진 제한

- v0.1.6부터 위젯은 숫자와 함께 '사용/남음'을 표시합니다. 위젯이 자체 선택을 갖지 않으면 앱 전역 설정을 따릅니다. v0.1.5까지는 위젯 기본값이 '사용', 앱 기본값이 '남음'이어서 같은 값이 서로 보수로 보였습니다. 실기기 런처에서의 표시는 아직 확인하지 않았습니다.
- v0.1.6은 로그인 세션을 디스크에 기록합니다(`CookieManager.flush`). v0.1.5까지는 로그인 직후 WebView가 파기되면 세션이 사라져 매번 다시 인증해야 할 수 있었습니다.
- v0.1.6부터 호스트 허용 목록은 메인 프레임 이동에만 적용합니다. 기기 검증·captcha 챌린지가 도는 하위 프레임을 막아 Grok 로그인이 무한 대기하던 문제를 고쳤습니다. https가 아닌 scheme은 계속 모든 프레임에서 차단합니다. 실기기에서 Grok 검증 통과는 아직 확인하지 않았습니다.
- **앱 내 Google 로그인은 휴대폰에 저장된 Google 계정을 사용할 수 없습니다.** 계정 격리용 WebView 프로필은 Chrome 세션이나 Android 계정에 접근할 수 없으며, 이를 바꿀 방법이 없습니다. Google 2단계 인증의 번호 맞추기 알림이 오지 않는 것도 Google이 embedded 브라우저 로그인을 제한하기 때문이며 앱에서 해결할 수 없습니다. UA 위장으로 탐지를 피하지 않습니다. v0.1.6은 해당 페이지 진입 시 미리 경고하고 이메일 로그인을 안내합니다. ChatGPT는 이메일+비밀번호, Grok은 X 계정 또는 이메일 로그인을 사용하세요.

- v0.1.4의 수동 붙여넣기 기능은 사용자 목표에 맞지 않아 v0.1.5에서 제거했습니다. 로그인 창을 닫는 것만으로 신규 계정과 세션을 삭제하지 않습니다.

- v0.1.4는 인증 팝업의 계정 프로필과 opener를 유지하고 로그인 후 화면 변화를 관찰합니다. Google 인증 알림 도착과 Grok 기기 검증 성공은 실기기에서 아직 검증하지 않았습니다. Google 정책상 앱 내 OAuth가 제한될 수 있어 소스 수정만으로 성공을 보장할 수 없습니다.
- 한국어 Codex `5시간 사용 한도 / 45% / 남음`, `주간 사용 한도 / 66% / 남음`, `남은 크레딧 / 0`을 회귀 fixture로 추가했습니다. 한국어 초기화 시각은 휴대폰 기본 시간대를 기준으로 해석합니다.

- 실제 Provider 계정으로 로그인·조회한 결과는 아직 검증하지 않았습니다. 공식 API 구현의 상태는 OFFICIAL_UNVERIFIED입니다.
- 소비자 연결은 사용자가 화면에 표시한 내용만 읽는 실험적 parser입니다. 웹 UI 변경이나 표현 차이로 읽기가 실패할 수 있습니다. private endpoint나 구독 OAuth token을 사용하지 않습니다.
- 일부 소셜 로그인은 embedded WebView를 차단합니다. 차단을 우회하지 않으며 이메일 로그인이 가능한지는 서비스별로 다릅니다. Custom Tabs는 격리 WebView profile에 세션을 넘기지 못해 Usage 수집 경로로 쓰지 않습니다. GeckoView는 아직 넣지 않았습니다.
- v0.1.3부터 ChatGPT Consumer는 일반 홈이 아니라 Codex Usage(`https://chatgpt.com/codex/settings/usage`)를 읽습니다. Grok은 `https://grok.com/?_s=usage`입니다. 실기기에서 숫자를 읽기 전에는 완료가 아닙니다.
- WebView MULTI_PROFILE 미지원 환경에서는 공유 세션으로 대체하지 않고 웹 계정 추가를 막습니다.
- 초기 parser는 제한된 한국어/영어 label을 인식합니다. 명시적 timezone을 가진 ISO 시각과 상대 시간 외의 모호한 날짜는 unknown으로 유지합니다.
- OpenAI API는 completions 활동만, Anthropic API는 messages 토큰만, xAI API는 팀 비용만 조회합니다. 전체 API 상품이나 결제 청구서를 대표하지 않습니다. 조회 구간은 UTC 오늘입니다.
- v0.1.5는 소비자 계정을 WorkManager 자동 수집에 포함합니다. 웹 계정은 15분 주기로 예약하며, 절전/네트워크/서비스의 재인증 요구에 따라 지연 또는 실패할 수 있습니다. 상시 정확한 5분 백그라운드 실행을 보장하지 않습니다.
- 위젯의 1x1~5x5는 launcher 셀 수 보장이 아니라 실제 dp 크기를 기준으로 대응합니다. RING_OPTIONAL은 bitmap 없이 bar로 대체합니다.
- 스냅샷 원본 신뢰도와 오래된 데이터 상태를 분리합니다. 최신 조회 실패 시 마지막 성공 데이터가 남습니다.
- 배포 서명 키는 저장소에 포함하지 않습니다. GitHub Release의 v0.1.2부터는 Actions Secret의 고정 upload key로 서명합니다.
- v0.1.0과 v0.1.1은 runner마다 다른 debug 키로 서명되어 서로, 그리고 v0.1.2와 덮어쓰기 설치가 되지 않습니다. v0.1.2를 쓰려면 기존 앱을 한 번 삭제한 뒤 새로 설치해야 합니다. v0.1.2 이후 버전은 같은 키로 업데이트할 수 있습니다.

- 이 환경에서는 에뮬레이터/실기기가 없어 instrumentation과 위젯 런처 검증을 실행하지 못했습니다.
- Provider 설명·bucket label 일부와 parser 메시지는 아직 코드 문자열입니다. UI 리소스 EN/KO와 완전히 분리되지 않았습니다.

기기별 테스트와 미실행 항목은 BUILD_REPORT.md에 기록합니다.
- v0.1.7은 로그인 브라우저의 동작을 진단용으로 기록합니다(설정 → 진단 → 로그인 브라우저 추적). URL은 scheme·host·path만 남기고 쿼리를 버리며, 이메일과 긴 토큰을 치환합니다. 페이지 본문과 쿠키는 기록하지 않습니다. 내보내기로 공유할 때 이 범위를 확인하세요.
- v0.1.7은 리다이렉트 이후 열리는 인증 팝업을 허용합니다. v0.1.6까지는 사용자 제스처가 없는 창을 거부해 일부 로그인이 대기 상태로 남을 수 있었습니다.
- v0.1.7의 로그인 화면 상단은 손잡이를 쓸어올려 접을 수 있습니다. 팝업이 없을 때 인증 버튼은 “로그인 다시 시작”으로 바뀝니다.
- v0.1.8부터 진단 화면과 내보내기는 로그인 브라우저 추적을 맨 위에 둡니다. v0.1.7에서는 동기화 로그 아래에 있어 찾기 어려웠습니다. 내보내기의 동기화 로그는 최근 20건으로 제한합니다.
- v0.1.8의 로그인 화면 상단은 손잡이를 아래로 쓸어내리면 숨고 위로 쓸어올리면 나옵니다. 전환에 220ms 애니메이션이 적용됩니다.
- v0.1.9는 OAuth 종단 페이지(`/oauth-complete` 등)가 메인 프레임에서 4초 이상 머물면 provider Usage 화면으로 이동합니다. 이 페이지는 팝업을 닫고 opener에 알리는 용도라 메인 프레임에서는 갈 곳이 없습니다. 메인 WebView의 `window.close()`도 같은 경로로 처리합니다. 실기기에서 Grok 로그인 완료는 아직 확인하지 않았습니다.
- v0.1.10은 grok 로그인이 세션 쿠키를 심는 `auth.grok.com`을 허용합니다. v0.1.9까지는 이 이동을 메인 프레임 차단으로 막아 로그인 후에도 grok.com이 미인증(401) 상태였습니다. 실기기 로그인 완료는 아직 확인하지 않았습니다.
- v0.1.11은 Grok 실제 페이지("매주 SuperGrok 한도", "중고"=used, "추가 사용 크레딧 US$..." )를 파싱합니다. v0.1.10까지는 로그인돼도 값을 못 읽었습니다.
- v0.1.11부터 위젯·앱의 게이지 막대는 배터리처럼 항상 남은 용량을 채웁니다(안 쓰면 꽉 참, 다 쓰면 빔). 숫자 텍스트의 사용/남음 선택과 무관합니다.
