# 현재 제한과 검증 범위 — v0.2.3

이 문서는 현재 상태를 설명한다. 과거에 해결된 문제와 시행착오는 [개발 이력](docs/DEVELOPMENT_HISTORY.md)에 있다.

- Codex와 Grok은 이전 사용자 실기기에서 수집 성공이 기록되어 있다. 이번 로컬 에뮬레이터 검증은 synthetic HTML·Demo 데이터·테스트 쿠키를 사용했으며 실제 계정으로 새로 로그인하지 않았다. Claude 및 OpenAI/Anthropic/xAI 공식 API 실계정 조회는 여전히 별도 확인이 필요하다.
- 소비자 수집은 공식 웹 화면의 DOM/접근성/CSS 표시값을 읽는 실험적 파서다. 서비스 화면 변경이나 인증 정책으로 실패할 수 있다. 의미를 확인할 수 없는 퍼센트는 used로 추측하지 않으며, 값이 없다는 뜻은 0%라는 뜻이 아니다. private endpoint나 구독 OAuth 토큰을 추출하지 않는다.
- 계정별 WebView MULTI_PROFILE 지원이 필요하다. 미지원 환경에서 세션을 공유하는 방식으로 대체하지 않는다. 휴대폰 Chrome/Google 계정의 세션을 가져오지 않으며 embedded 브라우저의 Google 인증 정책을 우회하지 않는다.
- WebView는 사용 중이거나 프로세스에 로드한 프로필 디렉터리를 바로 삭제하지 못한다. 로그아웃/삭제 시 해당 프로필의 쿠키·웹 저장소를 지우고 이름 재사용을 차단한다. 남은 디렉터리는 다음 프로세스 시작 때 삭제한다. 구형 WebView의 웹 저장소 삭제는 비동기이고 네트워크 캐시의 물리 제거도 다음 시작 때 완료된다. 다른 계정의 전역 쿠키/저장소는 지우지 않는다.
- 자동 갱신은 웹 계정 15분 예약, 앱 전경 5분 주기다. Android 절전·네트워크·재인증에 따라 지연된다. 설정의 자동 갱신 끄기/Wi-Fi 조건을 따른다. 수동 갱신은 오프라인에서도 작업을 시작해 오류를 표시하며, 기존 성공 데이터는 보존한다.
- 위젯 링 숫자는 항상 남은 %다. 기본 4×1은 링·별칭, 충분한 높이가 있을 때 초기화 캡션을 보여 준다. 새로고침 버튼은 폭 120dp 이상에서 표시된다. 전체 계정 처리 완료/실패/취소 후 갱신 표시를 해제한다. Android 런처별 셀 크기와 배터리 최적화는 에뮬레이터와 다를 수 있다.
- Grok reset은 해당 항목 뒤 최대 24줄에서 찾는다. 계정별 제품/언어/레이아웃 차이는 추가 실제 진단 로그가 필요할 수 있다. 화면에 시각이 없거나 모호하면 만들지 않는다.
- 공식 API 조회는 UTC 오늘의 OpenAI completions, Anthropic messages, xAI 팀 비용 범위다. consumer 구독 잔여 한도와 다른 데이터이며, API 토큰·비용에서 구독 잔여 %를 계산하지 않는다. Gemini/Perplexity consumer 연결은 미지원이다.
- 배포본과 같은 서명은 GitHub Actions secrets로만 만든다. 로컬 debug APK는 배포본과 서명이 달라 덮어쓰기되지 않는다. v0.1.2 이후 Actions 서명은 동일하고, v0.1.0/v0.1.1의 임시 debug 서명은 호환되지 않는다.
- 진단은 URL 쿼리·이메일·긴 토큰을 제거한다. 제한된 사용량 라벨 주변 텍스트는 진단에 포함될 수 있다. 비밀번호/쿠키를 직접 기록하지 않는다.
- Lint의 기존 스타일·사용 권장사항 경고는 남아 있다. 현재 검증 결과와 실제 캡처는 [v0.2.3 검증 기록](docs/QA_V023.md)을 참고한다.
